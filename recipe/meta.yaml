{% set name = "laxpy" %}
{% set version = "0.2.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/brycefrank/{{ name }}/archive/{{ version }}.tar.gz
  sha256: 96bbf70063b196c1112ec1667fc64fffbd9e3d9d6d52e6e7e1bf737b7f70428b

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"

requirements:
  host:
    - python >=3.6
    - pip
  run:
    - geopandas
    - laspy
    - lastools
    - numba
    - numpy
    - shapely
    - python >=3.6

test:
  imports:
    - laxpy

about:
  home: https://github.com/brycefrank/laxpy
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  summary: 'A las spatial indexing implementation in Python.'

  # The remaining entries in this section are optional, but recommended.
  description: |
    laxpy reads and parses .lax files generated from lasindex and provides
    an API for querying .las/.lax pairs with shapely geometries.
  dev_url: https://github.com/brycefrank/laxpy

extra:
  recipe-maintainers:
    - brycefrank
